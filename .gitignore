# Rust / Odra
contracts/**/target/
**/Cargo.lock
*.wasm

# .NET
[Bb]in/
[Oo]bj/
*.user
# ...but the Odra contract bin/ holds source build scripts (not .NET output) — keep it
!contracts/**/bin/
!contracts/**/bin/**

# Node / Angular
node_modules/
frontend/dist/

# OS / tooling junk
*.stackdump
Thumbs.db
.DS_Store

# Secrets & keys
**/secrets/
*.pem
*.private.key
spike/**/Config/*.local.json
.env

# Claude Code / AI planning artifacts — keep LOCAL, never publish
docs/superpowers/
.claude/
CLAUDE.md
.claude-*
