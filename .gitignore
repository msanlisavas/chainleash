# Rust / Odra
contracts/**/target/
**/Cargo.lock
*.wasm

# .NET
[Bb]in/
[Oo]bj/
*.user
# ...but the Odra contract bin/ holds source build scripts (not .NET output) — keep it
!contracts/**/bin/
!contracts/**/bin/**

# Node / Angular
node_modules/
frontend/dist/
frontend/**/.angular/

# Dashboard build output (produced by `ng build` into the agent host)
backend/ChainLeash.Agent/wwwroot/

# Agent runtime persistence (audit-feed snapshot — survives restarts, not source)
**/data/feed.json
backend/ChainLeash.Agent/data/

# Playwright MCP artifacts
.playwright-mcp/

# OS / tooling junk
*.stackdump
Thumbs.db
.DS_Store

# Secrets & keys
**/secrets/
*.pem
*.private.key
spike/**/Config/*.local.json
**/appsettings.local.json
.env

# Claude Code / AI planning artifacts — keep LOCAL, never publish
docs/superpowers/
.claude/
CLAUDE.md
.claude-*
